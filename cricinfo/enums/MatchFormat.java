package cricinfo.enums;

public enum MatchFormat {
    T20,    // 20 overs per innings, 1 innings per team
    ODI,    // 50 overs per innings, 1 innings per team
    TEST    // unlimited overs, up to 2 innings per team (4 total)
}
